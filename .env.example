# Vite dev proxy target for relative /api requests.
VITE_BACKEND_URL=http://localhost:5168

# Optional: set only when intentionally bypassing the Vite proxy.
# Direct cross-origin calls require the backend CORS policy to allow the Vite origin.
# VITE_API_BASE_URL=http://localhost:5168
